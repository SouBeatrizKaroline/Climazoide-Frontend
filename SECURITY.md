# Segurança

A versão mantida é a mais recente da branch `main`.

Não publique tokens, dados pessoais ou detalhes exploráveis em issues. Use **Security > Report a vulnerability** neste repositório. O frontend não deve conter segredos: variáveis `VITE_*` ficam públicas no navegador.
